import React from 'react';

const ShopItemDescription = ({description}) => {
    return (
        <article>
            <p>{description}</p>
        </article>
    );
};

export default ShopItemDescription;
