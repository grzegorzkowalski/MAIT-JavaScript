const HoverEvent = () => {
    return (
        <>
            <button onMouseEnter={ () => console.log("Najechano na przycisk")}>HoverEvent</button>
        </>
    );
};

export default HoverEvent;
