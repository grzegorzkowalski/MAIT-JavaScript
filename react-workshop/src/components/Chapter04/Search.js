import React from "react";

const Search = () => {
    return (
        <div>
            <input type="search" />
            <button>Wyszukaj</button>
        </div>
    );
};

export default Search;
